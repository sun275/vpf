# Classification

ImageNet training uses the YAML configs in `configs/` and the shared
`vpf.models.VPF` implementation. Run `main.py` from the repository root as shown
in the main README. `interpolate4downstream.py` remains available for legacy
checkpoint conversion.
