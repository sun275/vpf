# Autonomous driving

This task contains BDD100K and ACDC detection configs plus ACDC and Cityscapes
segmentation configs. Use this directory's `train.py`/`test.py` for detection.
Use `tasks/segmentation/train.py` or `test.py` with a config below
`configs/segmentation/` for driving segmentation.
